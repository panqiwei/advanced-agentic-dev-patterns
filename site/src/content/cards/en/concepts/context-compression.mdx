---
schemaVersion: one-shot-card/v1
cardLayout: bleed
title: Context Compression
titleAlt: Context Compression
tagline: the quality / compression-rate / readability trade-off across three strategies
refs:
  - concepts/context-management
  - concepts/long-running-agents
  - concepts/context-rot
sourceLine: Factory Evaluating Context Compression · 36000+ real sessions
---

import Band from '~/components/card/blocks/Band.astro';
import Kicker from '~/components/card/blocks/Kicker.astro';
import CoreDef from '~/components/card/blocks/CoreDef.astro';
import KV from '~/components/card/blocks/KV.astro';

<Band tone="ink" dense>
  <Kicker>CONCEPT · CONTEXT COMPRESSION · LONG-RUNNING AGENTS</Kicker>
</Band>

<Band tone="paper-raised" dense>
  <h2 class="card-title">Context Compression</h2>
  <p class="card-subtitle">Context Compression — compressing conversation history when the window runs out</p>
</Band>

<Band tone="tan">
  <CoreDef>Compressed context may lose critical details, causing the next session to misread state — compression is a double-edged sword. The three strategies each trade quality, compression rate, and interpretability differently.</CoreDef>
</Band>

<Band tone="paper-raised" grow>
  <div class="strat-table">
    <div class="st-header">
      <span>strategy</span><span>quality</span><span>ratio</span><span>readable</span><span>example</span>
    </div>
    <div class="st-row st-best">
      <span class="st-name">structured summary</span>
      <span class="st-val good">3.70 / 5.0</span>
      <span class="st-val mid">med</span>
      <span class="st-val good">high</span>
      <span class="st-val">Factory anchored</span>
    </div>
    <div class="st-row">
      <span class="st-name">full regeneration</span>
      <span class="st-val mid">3.55 / 5.0</span>
      <span class="st-val mid">med</span>
      <span class="st-val good">high</span>
      <span class="st-val">Anthropic SDK</span>
    </div>
    <div class="st-row st-worst">
      <span class="st-name">opaque compaction</span>
      <span class="st-val bad">3.35 / 5.0</span>
      <span class="st-val good">99.3%</span>
      <span class="st-val bad">none</span>
      <span class="st-val">OpenAI /compact</span>
    </div>
  </div>
</Band>

<Band tone="paper-sunk" dense>
  <div class="key-insight">
    <KV label="value of structure">explicit sections (intent / modifications / decisions / next steps) force retention — each section is a checklist that prevents silent loss</KV>
    <KV label="cost of opacity">compressed content can't be read or verified; technical details (paths, error messages) are the first to go</KV>
  </div>
</Band>

<Band tone="ink" dense>
  <div class="card-foot">
    <span class="foot-refs">→ Context Management · Long-Running Agents · Context Rot</span>
    <span class="foot-source">Factory (2025) · 36k+ sessions</span>
  </div>
</Band>

<style>{`
  .strat-table { width: 100%; }
  .st-header { display: grid; grid-template-columns: 1.4fr 0.7fr 0.7fr 0.7fr 1fr; gap: 3px; padding: 4px 0; border-bottom: 1px solid var(--rule); margin-bottom: 3px; }
  .st-header span { font-family: var(--font-mono); font-size: 0.57rem; color: var(--ink-faint); letter-spacing: 0.04em; }
  .st-row { display: grid; grid-template-columns: 1.4fr 0.7fr 0.7fr 0.7fr 1fr; gap: 3px; padding: 4px 0; align-items: center; border-bottom: 1px solid color-mix(in oklab, var(--rule) 30%, transparent); }
  .st-best { background: color-mix(in oklab, var(--celadon) 6%, transparent); border-radius: 3px; }
  .st-worst { background: color-mix(in oklab, var(--vermilion) 6%, transparent); border-radius: 3px; }
  .st-name { font-family: var(--font-han); font-size: 0.68rem; color: var(--ink); font-weight: 500; }
  .st-val { font-family: var(--font-han); font-size: 0.65rem; color: var(--ink-soft); }
  .good { color: var(--celadon) !important; font-weight: 500; }
  .mid { color: var(--gold) !important; }
  .bad { color: var(--vermilion) !important; }
  .key-insight { display: flex; flex-direction: column; gap: 2px; }
  .card-foot { display: flex; justify-content: space-between; align-items: center; gap: 8px; }
  .foot-refs { font-family: var(--font-mono); font-size: 0.58rem; color: currentColor; opacity: 0.75; letter-spacing: 0.04em; }
  .foot-source { font-family: var(--font-han); font-size: 0.65rem; color: currentColor; opacity: 0.55; }
`}</style>
