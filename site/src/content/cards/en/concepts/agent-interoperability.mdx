---
schemaVersion: one-shot-card/v1
cardLayout: bleed
title: Agent Interoperability
titleAlt: Agent Interoperability
tagline: From silos to a network · cross-framework agent collaboration
refs:
  - concepts/a2a-protocol
  - concepts/agent-card
  - concepts/orchestrator-workers
sourceLine: Google A2A Protocol · ASPLOS 2026
---

import Band from '~/components/card/blocks/Band.astro';
import Kicker from '~/components/card/blocks/Kicker.astro';
import CoreDef from '~/components/card/blocks/CoreDef.astro';

<Band tone="ink" dense>
  <Kicker>CONCEPT · AGENT INTEROPERABILITY · CROSS-FRAMEWORK</Kicker>
</Band>

<Band tone="paper-raised" dense>
  <h2 class="card-title">Agent Interoperability</h2>
  <p class="card-subtitle">From siloed ecosystems to a multi-agent network</p>
</Band>

<Band tone="tan">
  <CoreDef>Wrapping an agent as a tool is the common compromise — but it strips out multi-turn negotiation, stateful execution, and async delegation. Real interoperability treats agents as delegates, not functions.</CoreDef>
</Band>

<Band tone="paper-raised" grow>
  <div class="compare-grid">
    <div class="compare-col col-mcp">
      <Kicker>MCP (Agent ↔ tools)</Kicker>
      <ul class="compare-list">
        <li>Stateless function calls</li>
        <li>JSON Schema capability descriptions</li>
        <li>Single request-response</li>
        <li>Fits: search, databases</li>
      </ul>
    </div>
    <div class="compare-divider">VS</div>
    <div class="compare-col col-a2a">
      <Kicker>A2A (Agent ↔ Agent)</Kicker>
      <ul class="compare-list">
        <li>Stateful task delegation</li>
        <li>Agent Card capability discovery</li>
        <li>Async / multi-turn / resume</li>
        <li>Fits: delegating sub-tasks</li>
      </ul>
    </div>
  </div>
</Band>

<Band tone="paper-sunk" dense>
  <div class="principle-row">
    <span class="principle-num">①</span>
    <span class="principle-text">Standardized discovery: Agent Card lets any A2A-compliant client discover capabilities at runtime</span>
  </div>
  <div class="principle-row">
    <span class="principle-num">②</span>
    <span class="principle-text">Opaque execution: collaboration never exposes internal state — only declared capabilities and exchanged context</span>
  </div>
  <div class="principle-row">
    <span class="principle-num">③</span>
    <span class="principle-text">Specialized division of labor: each framework ships the agents it does best and plugs into the ecosystem via A2A</span>
  </div>
</Band>

<Band tone="ink" dense>
  <div class="card-foot">
    <span class="foot-refs">→ A2A Protocol · MCP · Orchestrator-Workers</span>
    <span class="foot-source">Google / Linux Foundation (2025)</span>
  </div>
</Band>

<style>{`
  .compare-grid { display: flex; gap: 0; align-items: stretch; height: 100%; }
  .compare-col { flex: 1; padding: 8px; }
  .compare-divider { display: flex; align-items: center; font-family: var(--font-mono); font-size: 0.7rem; color: var(--ink-faint); padding: 0 6px; }
  .col-mcp { background: color-mix(in oklab, var(--celadon) 10%, var(--paper-raised)); border-radius: 4px; }
  .col-a2a { background: color-mix(in oklab, var(--vermilion) 8%, var(--paper-raised)); border-radius: 4px; }
  .compare-list { list-style: none; padding: 0; margin: 6px 0 0; }
  .compare-list li { font-family: var(--font-han); font-size: 0.7rem; color: var(--ink-soft); padding: 2px 0; line-height: 1.35; }
  .compare-list li::before { content: "· "; color: var(--ink-faint); }
  .principle-row { display: flex; gap: 6px; align-items: baseline; margin-bottom: 3px; }
  .principle-num { font-family: var(--font-mono); font-size: 0.65rem; color: var(--vermilion); flex-shrink: 0; }
  .principle-text { font-family: var(--font-han); font-size: 0.7rem; color: var(--ink-soft); line-height: 1.4; }
  .card-foot { display: flex; justify-content: space-between; align-items: center; gap: 8px; }
  .foot-refs { font-family: var(--font-mono); font-size: 0.65rem; color: currentColor; opacity: 0.75; letter-spacing: 0.04em; }
  .foot-source { font-family: var(--font-han); font-size: 0.65rem; color: currentColor; opacity: 0.55; }
`}</style>
