---
interface Props { gap?: number; }
const { gap = 10 } = Astro.props as Props;
---
<div class="stack" style={`--stack-gap: ${gap}px;`}>
  <slot />
</div>
<style>
  .stack {
    display: flex;
    flex-direction: column;
    gap: var(--stack-gap);
  }
</style>
